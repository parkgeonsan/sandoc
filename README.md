# sandoc

**AI-powered Korean Government Business Plan Generator**
**AI 기반 정부 지원사업 사업계획서 자동 생성기**

> "서류 넣고, 대화하고, 완성본 받는다"

sandoc은 정부 지원사업 사업계획서(HWPX)를 자동 작성하는 AI 시스템입니다.
회사 정보 JSON을 입력하면, 9개 섹션의 사업계획서를 자동 생성하고 한글(HWPX) 문서로 출력합니다.

---

## Quick Start

```bash
# 1. 설치
git clone https://github.com/your-org/sandoc.git
cd sandoc
uv sync
source .venv/bin/activate
pip install -e .

# 2. 샘플 데모 실행 (가장 빠른 방법)
sandoc build --sample -o output/demo

# 3. 커스텀 회사 정보로 실행
sandoc build --company-info demo/sample_company.json -o output/my_plan

# 4. E2E 데모 스크립트 실행
bash demo/run_demo.sh
```

### 출력 결과

```
output/demo/
├── (주)스마트팜테크_사업계획서.hwpx   # 최종 HWPX 문서
├── plan.json                          # 생성된 계획 (구조화 데이터)
├── company_info.json                  # 회사 정보
├── sections/                          # 섹션별 마크다운
│   ├── 01_company_overview.md
│   ├── 02_problem_recognition.md
│   ├── ...
│   └── 09_funding_plan.md
└── prompts/                           # 각 섹션의 생성 프롬프트
    ├── prompt_company_overview.md
    └── ...
```

---

## CLI Commands

### `sandoc run` — 전체 파이프라인 실행

```bash
# 전체 파이프라인 (extract → visualize → review → assemble)
sandoc run projects/2026-창업도약패키지/

# 회사 정보 포함
sandoc run projects/my-project/ -c company.json

# 단계 건너뛰기
sandoc run projects/my-project/ --skip-extract --skip-review
```

### `sandoc interview` — 누락 정보 수집

```bash
# 설문지 + JSON 템플릿 생성
sandoc interview projects/2026-창업도약패키지/

# 작성된 답변 병합
sandoc interview projects/2026-창업도약패키지/ --fill answers.json
```

### `sandoc learn` — 지식 축적

```bash
# 초안에서 표현/패턴 추출
sandoc learn projects/2026-창업도약패키지/

# 지식 디렉토리 지정
sandoc learn projects/my-project/ -k ./my_knowledge/
```

### `sandoc inject` — HWP 삽입 매핑 생성

```bash
# 초안↔양식 매핑 + hwpx-mcp 지시서 생성
sandoc inject projects/2026-창업도약패키지/
```

### `sandoc generate` — 사업계획서 콘텐츠 생성

```bash
# 샘플 데모
sandoc generate --sample -o output/

# 회사 정보 JSON으로 생성
sandoc generate --company-info company.json -o output/

# 프롬프트만 생성 (LLM 연동 전 검토용)
sandoc generate --company-info company.json --prompts-only -o prompts/

# 전체 옵션
sandoc generate \
  --company-info company.json \
  --template template.hwp \
  --announcement announcement.pdf \
  --style style-profile.json \
  --output output/
```

### `sandoc build` — HWPX 문서 빌드

```bash
# 샘플 데모 → HWPX
sandoc build --sample -o output/

# 회사 정보 → HWPX
sandoc build --company-info company.json -o output/

# 스타일 미러링 (원본 HWP 서식 유지)
sandoc build --company-info company.json --style style-profile.json

# 기존 plan.json에서 HWPX만 빌드
sandoc build --plan plan.json -o output/
```

### `sandoc analyze` — 문서 분석

```bash
# HWP 양식 구조 분석
sandoc analyze template.hwp

# PDF 공고문 분석 (심사기준, 배점, 일정 추출)
sandoc analyze announcement.pdf -o analysis.json
```

### `sandoc profile` — 스타일 프로파일 추출

```bash
# HWP 파일에서 서식 프로파일 추출
sandoc profile template.hwp -o style-profile.json
```

### `sandoc classify` — 문서 자동 분류

```bash
# 폴더 내 문서를 카테고리별 분류
sandoc classify docs/
```

---

## Company Info JSON

회사 정보는 JSON 파일로 제공합니다. 예시: [`demo/sample_company.json`](demo/sample_company.json)

### 주요 필드

| 구분 | 필드 | 설명 |
|------|------|------|
| 기본정보 | `company_name`, `ceo_name`, `establishment_date` | 기업명, 대표자, 설립일 |
| 아이템 | `item_name`, `item_summary`, `product_description` | 창업아이템 정보 |
| 문제인식 | `problem_background`, `problem_statement` | 개발 동기, 문제 기술 |
| 시장분석 | `target_market`, `competitive_advantage` | 목표시장, 경쟁우위 |
| 재무 | `funding_amount`, `budget_items` | 정부지원금, 예산 항목 |
| 팀 | `team_members`, `ceo_background` | 팀 구성, 대표자 역량 |

전체 스키마는 [`src/sandoc/schema.py`](src/sandoc/schema.py)를 참조하세요.

---

## Architecture

```
┌──────────────────────────────────────────────────────────────┐
│  INPUT                                                        │
│  ├── company_info.json    (회사 정보)                        │
│  ├── template.hwp         (양식, 선택)                       │
│  ├── announcement.pdf     (공고문, 선택)                     │
│  └── style-profile.json   (스타일, 선택)                     │
└──────────────┬───────────────────────────────────────────────┘
               ▼
┌──────────────────────────────────────────────────────────────┐
│  CompanyInfo (schema.py)                                      │
│  └── JSON → dataclass 변환, 유효성 검증                       │
└──────────────┬───────────────────────────────────────────────┘
               ▼
┌──────────────────────────────────────────────────────────────┐
│  PlanGenerator (generator.py)                                 │
│  ├── 9개 섹션별 프롬프트 빌드 (prompts/*.txt 템플릿)          │
│  ├── 변수 치환 (회사정보 → 프롬프트)                          │
│  └── 콘텐츠 생성 (fill 모드 / 향후 LLM 연동)                 │
└──────────────┬───────────────────────────────────────────────┘
               ▼
┌──────────────────────────────────────────────────────────────┐
│  OutputPipeline (output.py)                                   │
│  ├── StyleMirror (서식 미러링)                                │
│  ├── HwpxBuilder (HWPX ZIP 조립)                             │
│  ├── validate_hwpx (구조 검증)                               │
│  └── 결과 저장 (HWPX + JSON + MD)                            │
└──────────────┬───────────────────────────────────────────────┘
               ▼
┌──────────────────────────────────────────────────────────────┐
│  OUTPUT                                                       │
│  ├── *.hwpx               (최종 한글 문서)                   │
│  ├── plan.json             (구조화 데이터)                    │
│  ├── sections/*.md         (섹션별 마크다운)                  │
│  └── prompts/*.md          (생성 프롬프트)                    │
└──────────────────────────────────────────────────────────────┘
```

### 9-Section Structure (2025 창업도약패키지)

| # | 섹션 | 평가항목 | 설명 |
|---|------|---------|------|
| 1 | 기업 개요 및 일반현황 | — | 기본 정보, 아이템 개요 |
| 2 | 문제인식 (Problem) | 문제인식 | 개발 동기, 필요성, 현황 |
| 3 | 목표시장 분석 | 실현가능성 | 시장, 고객, 경쟁 분석 |
| 4 | 사업화 추진 성과 | 실현가능성 | 매출 실적, 비즈니스 모델 |
| 5 | 사업화 추진 전략 | 성장전략 | 성장 전략, 마케팅 계획 |
| 6 | 자금운용 계획 | 성장전략 | 예산 구성, 비율 검증 |
| 7 | 기업 구성 (Team) | 팀구성 | 팀, 인프라, 지식재산권 |
| 8 | 재무 계획 종합 | 성장전략 | 재무 분석, 투자유치 가점 |
| 9 | 사업비 집행 계획 | 성장전략 | 비목별 상세 집행 계획 |

> 4대 평가항목(문제인식, 실현가능성, 성장전략, 팀구성) 각 60점 미만 시 탈락

---

## Development

### 요구사항

- Python 3.10+
- [uv](https://docs.astral.sh/uv/) (패키지 매니저)

### 설치 및 테스트

```bash
# 의존성 설치
uv sync
source .venv/bin/activate
pip install -e ".[dev]"

# 테스트 실행
python3 -m pytest tests/ -v

# 특정 테스트 실행
python3 -m pytest tests/test_e2e.py -v -k "TestE2EFullPipeline"
python3 -m pytest tests/test_output.py -v
```

### 데모 실행

```bash
# 전체 E2E 데모
bash demo/run_demo.sh

# 수동 실행
sandoc generate --sample -o demo/output
sandoc build --sample -o demo/output
```

### 프로젝트 구조

```
sandoc/
├── src/sandoc/                 # 메인 패키지
│   ├── cli.py                  # CLI (Click) — 15 commands
│   ├── schema.py               # CompanyInfo 데이터 모델
│   ├── generator.py            # 콘텐츠 생성 파이프라인
│   ├── hwpx_engine.py          # HWPX 빌드 엔진
│   ├── output.py               # 출력 파이프라인
│   ├── analyzer.py             # HWP/PDF 분석
│   ├── parser.py               # HWP 파서 (OLE2)
│   ├── style.py                # 스타일 프로파일 추출
│   ├── extract.py              # 문서 추출 파이프라인
│   ├── assemble.py             # 마크다운 → HWPX 조립
│   ├── visualize.py            # 시각화 차트 생성
│   ├── review.py               # 사업계획서 자가 검토
│   ├── interview.py            # 누락 정보 수집/병합
│   ├── learn.py                # 지식 축적 (표현/패턴)
│   ├── inject.py               # HWP 양식 삽입 매핑
│   ├── run.py                  # 전체 파이프라인 실행
│   ├── profile_register.py     # 기업 프로필 등록
│   └── prompts/                # 9개 프롬프트 템플릿
│       ├── 01_company_overview.txt
│       └── ...
│
├── tests/                      # 테스트 (266+)
│   ├── test_e2e.py             # E2E 파이프라인 테스트
│   ├── test_extract_assemble.py # Extract/Assemble 테스트
│   ├── test_visualize_review_profile.py # Visualize/Review/Profile 테스트
│   ├── test_interview_learn_inject_run.py # Interview/Learn/Inject/Run 테스트
│   ├── test_output.py          # HWPX 엔진 테스트
│   ├── test_parser.py          # HWP 파서 테스트
│   └── v1-data/                # 테스트 데이터
│
├── demo/                       # E2E 데모
│   ├── run_demo.sh             # 데모 스크립트
│   └── sample_company.json     # 샘플 회사 정보
│
├── .claude/agents/             # AI 에이전트 정의
├── config/                     # 설정
├── knowledge/                  # 축적 지식
│   ├── expressions/            # 효과적 표현 DB
│   ├── patterns/               # 구조 패턴 DB
│   └── lessons.md              # 프로젝트별 교훈 기록
├── profiles/                   # 스타일 프로파일 DB
└── pyproject.toml              # 프로젝트 설정
```

---

## AI Agent Team

| Agent | Role | 담당 |
|-------|------|------|
| 📋 분석관 (analyst) | 서류 분석 | 공고 파싱, 심사기준 매핑 |
| ✍️ 작성관 (writer) | 콘텐츠 작성 | 사업계획서 텍스트, 문체 관리 |
| 🎨 비주얼관 (visualist) | 시각 자료 | 차트, 다이어그램, 인포그래픽 |
| 📐 문서관 (docengine) | 문서 출력 | HWP 양식 조작, HWPX 출력 |

---

## License

MIT
